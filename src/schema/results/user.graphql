"Resultado que devuelve un User"
type ResultUser implements Result {
  message: String!
  status: Boolean!
  user: User
}

"Resultado que devuelve [User]"
type ResultUsers implements Result {
  status: Boolean!
  message: String!
  users: [User]
  info: ResultInfo
}
